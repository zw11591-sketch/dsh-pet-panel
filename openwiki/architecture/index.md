# Files

- [Build, Bundling, and Packaging](build-and-packaging.md) - How dsh-pet-panel compiles and ships its dual host/client halves — the tsc -b && tsdown && postbuild chain, the shared closure-factory browser bundle preset, the frozen platform module table, and why postbuild copies tsc-lowered output over the rolldown bundle.
- [Dual-Face Plugin Architecture](overview.md) - The top-level mental model for how dsh-pet-panel mounts into a DeepSeek Harness web profile as a Node host face (six cordis Gateways in src/index.ts exposing Typert remotes) plus a browser client face (src/client/index.ts slot components consuming them), wired together by the package.json bundle patch and the hand-written TYPERT_REMOTE manifest.
- [Client-to-Host Typert Remote Bridge](typert-remote-bridge.md) - The explicit RPC boundary between the dsh-pet-panel browser client bundle and the Node host bundle — the hand-written TYPERT_REMOTE manifest with strict zod codecs in src/client/remote.ts, the @Remote-decorated TypertRemoteService gateways in src/index.ts discovered in source mode, the { ok:true, value } | { ok:false, error } wire envelope, the unwrap()/compact() helpers, and the ordering constraint that the capability child plugin touches remote.<ns> only after $mount.
