# Files

- [A2A Protocol Surface](a2a-protocol.md) - The Agent-to-Agent integration on dsh-pet-panel — the per-profile a2a-agents.json card + external-agent registry backed by A2AConfigGateway over the Typert remote, the model-facing a2a_list_agents / a2a_call outbound tools with the exact→normalized→substring→Levenshtein resolution chain and candidate closure on no-unique-match, and the inbound /.well-known/agent-card.json + /a2a JSON-RPC message/send endpoints that assemble a system prompt from the card and stream a reply through llm.stream.
- [Per-Profile Data Isolation](per-profile-isolation.md) - The invariant that each dsh profile sees only its own skills, MCP servers, and A2A registrations, because dsh-pet-panel derives the active profile from process.argv and resolves every data surface under $DSH_HOME/profiles/<name>/.
- [Session Lifecycle Trace (zstd JSONL)](session-lifecycle-events.md) - How dsh-pet-panel's host face parses a dsh session's zstd-compressed JSONL execution log into a flattened LifecycleEvent stream and a LifecycleSnapshot for the Task Manager panel — findSessionFile, multi-frame zstd decompression, the event-kind switch, and the truncation / compact() boundaries.
