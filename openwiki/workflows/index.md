# Files

- [Browser Client Surfaces](client-surface.md) - The dsh-pet-panel browser face — how src/client/index.ts wires the conversation-view and shell-overlay slot components (floating PetView desk pet, DashboardView metrics tab, SkillForge/Tool/A2A capability panels, TeamChamber panel+trigger, BackgroundSwitcher), the Papergames brand/theme overlays, the /pet slash command, and the module-level petStore/teamStore cross-slot bridges.
- [Skill Forge (技能工坊)](skill-forge.md) - The end-to-end Skill Forge flow — the host SkillForgeGateway's five @Remote methods (list/read/write/delete/generate) over the Typert bridge, the per-profile skills/<name>/SKILL.md layout behind the NAME_RE path-traversal guard and assertName(), the frontmatter name/description parse with directory-name fallback, the generate() model selection that prefers the current default model and falls back to the first registered provider, and how the browser SkillForgeView wraps the mirrored SkillApi through the client remote and unwrap().
- [Team Panel and Message Routing Engine](team.md) - The team feature — how the host TeamGateway (listTeams / createTeam / updateTeam / deleteTeam / listThreads / openThread / getThread / send) persists teams.json and team-chats under the profile dir, how the client TeamTrigger and TeamView overlay are bridged by the module-level teamPanelStore, and how the @-mention routing engine decides which members a group or single-chat message reaches, including the replyAsSelf persona reply and the per-agent contextId multi-turn memory.
- [Tool Integrations (工具集成 / MCP)](tool-integrations.md) - The end-to-end Tool Integrations flow — the host ToolIntegrationsGateway's list/read/write/delete of per-profile mcp-servers.json backed by the McpConfig shape, the entryFor() loader entry derivation (id mcp-<serverName>, name @deepseek-ai/dsh-mcp-client), the startup mountAll() and write() hot-replace semantics where a save persists the config then unmounts the old loader entry and mounts a new one, the mounted map as the live-vs-stale source of truth, and how ToolIntegrationsView wraps the mirrored McpApi through the client remote and unwrap().
